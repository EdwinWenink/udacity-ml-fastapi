from src.ml.model import train_model


def test_model_fitting(get_Xy):
    # How to test `train_model`?
    # Check if the model is fitted afterwards?

    # Train model.
    # clf = train_model(clf, X_train, y_train)
    X, y = get_Xy
    X
    y
    pass
